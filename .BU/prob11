def product(x):
	product = 1
	for y in x:
		product *= y
	return product

def iterate2DList(x):
	for row in range(len(x)):
		for col in range(len(x[0])):
			x[i][j]

def searchDirAtPoint(row,col,board):
	dirs = [(dRow,dCol) for dRow in range(-1,2) for dCol in range(-1,2)]
	dirs.pop(5)
	for dir in dirs
		product = 1
		for i in range(4):
				
		
